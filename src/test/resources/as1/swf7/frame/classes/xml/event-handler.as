/*
 * Event Handlers.
 */
 
doc = new XML("variables.xml");

doc.onLoad = function(document)
{
    trace("file loaded");
};

doc.onLoad = function(document)
{
    trace("file loaded");
};