- test: trace()
  refid: trace
  file: functions/trace.as
  script: |
    trace(a);
- test: getVersion()
  refid: getversion
  file: functions/getversion.as
  script: |
    x = getVersion();
- test: random()
  refid: random
  file: functions/random.as
  script: |
    x = random(a);
- test: stopAllSounds()
  refid: stopallsounds
  file: functions/stopallsounds.as
  script: |
    stopAllSounds();
- test: toggleHighQuality()
  refid: togglehighquality
  file: functions/togglehighquality.as
  script: |
    toggleHighQuality();
- test: showRedrawRegions()
  refid: showredrawregions
  file: functions/showredrawregions.as
  script: |
    _global.showRedrawRegions(true);
- test: Object()
  refid: object
  file: functions/object.as
  script: |
    x = Object(1);
    