- test: throw an error
  refid: throw
  file: throw_object.as
  profiles:
    - DEFAULT_1_4
  script: |
    if (a != 0) {
        throw new Error("Invalid value");
    }
- test: throw a variable
  refid: throw
  file: throw_variable.as
  profiles:
    - DEFAULT_1_4
  script: |
    if (a != 0) {
        throw b;
    }
-