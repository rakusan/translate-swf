- test: Mouse addlistener()
  refid: mouse_addlistener
  file: classes/mouse/mouse_addlistener.as
  script: |
    listener = new Object();
    listener.onMouseDown = function() {};
    listener.onMouseUp = function() {};
    listener.onMouseMove = function() {};
    Mouse.addListener(listener);
- test: Mouse removelistener()
  refid: mouse_removelistener
  file: classes/mouse/mouse_removelistener.as
  script: |
    listener = new Object();
    listener.onMouseDown = function() {};
    listener.onMouseUp = function() {};
    listener.onMouseMove = function() {};
    Mouse.addListener(listener);
    Mouse.removeListener(listener);
- test: Mouse wheel
  refid: mouse_wheel
  file: classes/mouse/mouse_wheel.as
  script: |
    listener = new Object();
    listener.onMouseWheel = function(delta, target) { };
    Mouse.addListener(listener);
- test: Mouse hide()
  refid: mouse_hide
  file: classes/mouse/mouse_hide.as
  script: |
    Mouse.hide();
- test: Mouse show()
  refid: mouse_show
  file: classes/mouse/.as
  script: |
    Mouse.show();
